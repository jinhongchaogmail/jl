12月23日
总结一下这几天的工作。
这几天因为显卡的CUDA不能支持神经网络的反向传播，在反复尝试换驱动换系统的时候，把两三天的工作弄丢了，现在只找回来一部分。
但是在折腾中也搞明白了一些事情。在此总结一下。
1.960显卡无论换哪种系统哪种驱动，哪个版本的cuda_toolkit.都不支持反向传播。只能用CPU训练。
2.发现股票数据训练起来还不随机数训练的损失曲线好。
3.但是之前读取那个csv上的数据还是不错。这是什么原因呢。得想办法搞明白。
4.最有收获的还是在处理数据时，找到了很单的方式。用不多的代码就可以搞定。
5.在做实验的时候，发现神经网络第二层设置为10个连接以及14个最好。当时用的三个特征。
7.多线程也很容易实现，只需要在for之前加上＠thread即可。但是对于读写文件这样的操作还是不行。
8.AI教了我这个函数。可以替换last .x -> x[1, end, :]很好玩。

9现在来做实验，看哪个特征最好。


